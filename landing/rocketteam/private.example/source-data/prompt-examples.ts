// Placeholder. The runtime prefers ../private/source-data/prompt-examples.ts
// when present. These strings are interpolated into LLM prompts as illustrative
// fragments; the real names appear here so the model produces examples in the
// same shape as your real team data.
export const REPORT_AGENT_RATIONALE_EXAMPLE =
  '"R1 张三 BID cap=8" "R2 赵六 RECOMMEND_SPLIT"';

export const REPORT_CHAT_ACTION_CITE_EXAMPLE =
  'R2 张三 SPLIT 把视觉派给 赵六';
